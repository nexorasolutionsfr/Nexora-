# Plan n8n — inventaire vérifié et architecture retenue

14 septembre 2026. Relevé sur **l'instance vive** (conteneur `nexora-n8n`,
`n8n export:workflow --all`, lecture seule, 20 définitions) et sur les exports
versionnés (`n8n/socle-envois/*.json`, `n8n/assistant-garage/*`). Rien n'a été
importé, activé ni modifié dans l'instance vive.

## 1. Ce qui tourne réellement (instance vive, 14 sept. 2026)

**6 actifs sur 20.** Les autres sont archivés (n8n `isArchived`, non
réactivables sans désarchivage) ou inactifs.

| Chemin | Workflow vif | Actif | Déclencheur | Source réelle | Résolution du garage | Destinataire | Garde d'autorisation | États | Service externe | Export versionné | Preuve | Coût | Dépendance restante |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| Nouveau devis | `Nouveau devis (socle)` `X39OgaEUulqhv1hO` | **oui** | cron `*/2` (≈720 exécutions/jour) | `notifications_devis` via `reserver_notifications('devis')` | par le document (`devis.garage_id`), tous garages | `destinataire_valide` figé à l'autorisation | `autoriser_envoi_devis` (dirigeant/accueil) ; depuis `20260919000100` refuse le chiffrage incomplet ; `empreinte_devis` intègre lignes + état | sans_lien → en_attente → envoi_en_cours → envoye / bloque / a_reprendre | SMTP Brevo (30/h) | `n8n/socle-envois/nouveau-devis.json` | envoi réel prouvé le 8-9 sept. (mémo) | Brevo gratuit ; Mac hôte | hébergement permanent (Mac endormi = rien ne part) |
| Facture | `Facture (socle)` `9IG1g2ZmHQzhnsMS` | **oui** | cron `*/2` | `notifications_factures` | idem | idem | `autoriser_envoi_facture` | idem | SMTP Brevo | `facture.json` | preuve d'envoi Brevo **à faire** (mémo 12 sept.) | idem | idem |
| Proposition de RDV | `Proposition RDV (socle)` `HdO63GrT2WfopDQP` | **oui** | cron `*/2` | `notifications_proposition` | idem | e-mail client | autorisation à vérifier dans le code du socle (hors périmètre de cette nuit) | idem | SMTP Brevo | `proposition-rdv.json` | mémo 8 sept. : marquait « envoyé » sans e-mail — corrigé dans le socle du 9 | idem | idem |
| Véhicule prêt | `Véhicule prêt (socle)` `jXsssqkdKFR3Hnf9` | **oui** | cron `*/2` | `notifications_atelier` | idem | `destinataire_valide` | `autoriser_envoi_atelier` (depuis 20260918000300, naît `sans_lien`) | idem | SMTP Brevo | `vehicule-pret.json` | envoi réel le 11 sept. (mémo) | idem | **deux composeurs** (voir §3) |
| Détection no-show | `3 - Détection no-show` `jnDj8Aj9DmuLV5Di` | oui | cron horaire | `rendez_vous` | ? (4 nœuds, pas d'envoi) | aucun envoi | — | — | — | non versionné | aucune | — | à documenter ou archiver |
| Journal d'erreurs | `3 - Journalisation erreurs` | oui | `errorTrigger` | erreurs n8n | — | aucun envoi | — | — | — | non versionné | — | — | — |
| Confirmation / modification / annulation RDV | — | **non** (archivé dans `Assistant Garage`) | — | `confirmations_rappels_file`, `confirmations_jetons` existent en base | — | — | — | — | — | `n8n/assistant-garage/*` | aucune | — | activation = tout l'Assistant |
| Acceptation / refus du devis | — (branches `webhook /devis-accepte`, archivées) | **non** | — | depuis 20260917000100 la réponse naît `sans_lien` dans `notifications_devis` (type accepte/refuse) et passe par le socle « Nouveau devis » **si autorisée** | par document | idem | `autoriser_envoi_devis` | idem | — | — | — | — | — |
| Relances (devis sans réponse, travaux différés) | — | **non** | — | **nouveau** : `relances_travaux` (20260919000400) | par `travaux_differes.garage_id` | `destinataire_valide` figé | `autoriser_envoi_relance_travail` | a_relire → en_attente → envoi_en_cours → envoye / bloque / a_reprendre ; obsolete / annulee | SMTP Brevo (Prod) / simulé (Test) | `n8n/relances-travaux/{test,production}.json` | 32 contrôles serveur (`relances-travaux-serveur.mjs`) ; **workflow non exécuté dans n8n** | 0 | import dans une instance de recette, puis revue |
| Demande d'avis Google | — (branche `scheduleTrigger 0 9` de l'Assistant, archivée) | **non** | — | `rendez_vous.avis_demande` | — | — | aucune (échouait chaque jour faute de destinataire, mémo) | — | — | — | — | — | lien d'avis par garage + retrait persistant + règles officielles : **non commencé**, branche indisponible |
| Demandes entrantes IMAP / formulaire / WhatsApp | — (`emailReadImap`, `webhook /demande-site`, `/demande-whatsapp` de l'Assistant, archivées ; `RECETTE — Assistant Garage` ×2 inactifs) | **non** | — | boîte Gmail `lkckirua@…` (codée en dur) | **par l'expéditeur du message** — inacceptable (voir §4) | alerte interne vers `lkckirua@…` | aucune | — | Gmail/IMAP, Twilio non configuré | `n8n/assistant-garage/*` | preuves du 10 sept. (mémo) | Gmail | connecteur authentifié **par garage** ; entrées = données non fiables |
| Alerte interne | — | **non** | — | — | — | `lkckirua@…` en dur | — | — | — | — | — | — | — |

Ce que l'inventaire corrige par rapport aux anciens rapports : le nombre de
workflows (20, dont 6 actifs, dont 2 sans envoi) ; « Véhicule prêt » **est**
actif ; les branches confirmation/annulation/avis/IMAP ne tournent **pas**.

## 2. Architecture retenue, appliquée à UN chemin

La base garde documents, autorisations, files et états. Le dashboard prépare,
fait relire, autorise, affiche. n8n **réserve** ce qui est autorisé et
**transporte** ; il ne recalcule aucune règle, ne résout aucun garage, ne
compose aucun message.

Le chemin représentatif : **la relance d'un travail différé**
(`20260919000400_relances_travaux_differes.sql`).

```
candidat        travaux_differes échu (statut planifie / a_relancer, date_relance ≤ aujourd'hui)
brouillon       preparer_relances_travaux()      → relances_travaux.a_relire   [service_role]
autorisation    autoriser_envoi_relance_travail() → en_attente                 [dirigeant / accueil]
réservation     reserver_relances_travaux()      → envoi_en_cours (SKIP LOCKED) [service_role]
tentative       n8n : SMTP (prod) / Code simulé (test)
résultat        terminer_relance_travail()       → envoye | bloque | a_reprendre (→ en_attente, tentatives < 3)
```

Contrat d'une ligne : `garage_id` (identité du garage), `travail_differe_id`
+ `echeance` (identité d'idempotence — une relance par travail et par
échéance ; un report crée une autre échéance, donc une autre relance
possible, jamais un doublon accidentel), `statut`, `motif` /
`derniere_erreur`, `destinataire_valide`, `empreinte_document`,
`autorise_par`, `tentatives`.

Conditions **relues à la réservation** : travail encore ouvert, échéance
inchangée, destinataire inchangé, message inchangé. Ce qui a bougé est mis de
côté (`bloque`) avec le motif, et **revalidé à la main**.

`envoi_en_cours` n'est jamais recyclé par un délai. Un timeout après une
acceptation possible du fournisseur reste incertain ; l'écran dit « Envoi à
vérifier ». Pas de promesse « exactement une fois » avec SMTP.

**Une seule composition** : `composer_relance_travail()` en base, au moment de
la préparation. L'aperçu de l'écran et l'e-mail sont le même texte. C'est la
réponse au constat des « deux composeurs » de « véhicule prêt ».

### Réveil événementiel : non testé, et pourquoi

Les webhooks de base Supabase peuvent réveiller un consommateur, mais ils ne
font pas une file durable, et le Mac hôte endormi ne reçoit rien. Le workflow
livré est une **relève périodique bornée** (`*/15`), qui retrouve ce qui a été
manqué. Aucun tunnel public n'a été ouvert. Le réveil événementiel est
documenté comme **non testé**.

### Mesure

Sur Test, le chemin a été joué par script (`relances-travaux-serveur.mjs`),
pas par n8n : 32 contrôles, dont la concurrence de réservation et le blocage
après modification. **Nombre d'exécutions n8n utiles/vides : non mesuré**, le
workflow n'ayant pas tourné. À `*/15`, une tournée fait 96 exécutions/jour par
workflow, la plupart vides ; c'est le coût à mesurer sur une fenêtre connue
avant toute extrapolation.

## 3. Les deux composeurs de « véhicule prêt » — revérifié

Toujours vrai au 14 septembre : le nœud Code du workflow vif
(`jXsssqkdKFR3Hnf9`) et `apercu_message_atelier` (SQL) composent chacun le
message. `scripts/recette/compare-apercu-atelier.mjs` les compare (0 écart le
13 sept.). Ce lot **n'a pas** modifié ce chemin : correction à faire en
faisant lire au nœud n8n le texte rendu par la base (comme pour les relances),
avec validation fraîche de la version et du destinataire.

## 4. IMAP, avis Google : conditions, pas d'interrupteur

- **IMAP** : pas de garage de secours, pas de routage sur l'expéditeur libre.
  L'identité de la boîte/connecteur autorisé doit déterminer le garage ; un
  transfert partagé ne prouve rien. Messages et pièces jointes = entrées non
  fiables, jamais des instructions. Activation après connecteur authentifié
  **par garage** et recette de livraison réelle. **Rien de livré cette nuit.**
- **Avis Google** : lien d'avis propre au garage (`garages.lien_avis_google`
  existe), retrait persistant (n'existe pas), règle d'éligibilité, aucune
  sélection des seuls satisfaits, aucune récompense. **Non commencé** ; la
  branche reste indisponible (`CAPACITES.demandeAvis.disponible = false`).

## 5. Exports livrés

- `n8n/relances-travaux/test.json` — Supabase **Test**, identifiant
  `RPC Supabase RECETTE (Test)` (`BmVzKJSWPhAJ5V3Z`), **borné au garage de
  recette** (`p_garages`), transport **simulé** (nœud Code : refuse tout
  destinataire hors `.invalid`, n'envoie rien, clôt `envoye` avec le motif
  « transport simulé »). Inactif à l'import (n8n importe tout inactif).
- `n8n/relances-travaux/production.json` — Supabase Production,
  `RPC Supabase Production` + `SMTP Brevo — envois métier`, **inactif**,
  tous garages (`p_garages: null`), `onError: continueErrorOutput` sur l'envoi
  → `a_reprendre` borné.
- Générés par `n8n/relances-travaux/construire.mjs` : un seul source, deux
  variantes. Identifiants de workflow **que rien ne porte** dans l'instance
  vive (`relancestravaux{test,prod}0000001`), aucune clé dans les fichiers.

Vérifié dans les fichiers : URL (Test/Prod), identifiants distincts,
déclencheur `*/15`, garde `p_garages` sur Test, absence de collision d'id.
**Non vérifié** : l'import et une exécution dans une instance n8n de recette.

## 6. Conditions de passage en Production (pour le plan de publication)

1. Migrations `20260919000300` puis `20260919000400` (aucune donnée transformée).
2. Importer `production.json` dans un workflow **neuf** (« Import from File »,
   jamais collé dans un canvas existant), vérifier les deux identifiants,
   laisser **inactif**.
3. Avant activation : compter `relances_travaux` (0 attendu), lancer
   `preparer_relances_travaux()` **une fois à la main** avec `p_garages` sur
   un garage volontaire, relire les brouillons produits, n'autoriser que ce
   que le garage a relu. La population susceptible de recevoir un message =
   les clients des travaux différés échus **autorisés à la main**, aucun
   autre.
4. Activation, puis observation d'une fenêtre connue (exécutions utiles /
   vides). Arrêt sûr : désactiver le workflow ; les lignes `en_attente`
   restent en attente, rien n'est perdu, rien ne part.
5. Retour arrière : désactiver, puis (optionnel) supprimer la table et les
   fonctions ; `travaux_differes` n'a pas bougé.

## 7. Sauvegarde, restauration, exécutions

Instance vive : SQLite dans le conteneur, sauvegarde par `n8n export:workflow
--all --backup` (dernier export complet : `Desktop/Nexora - Automatisation/
Workflows n8n/export-complet-2026-09-11/`) et copie du volume. Pile cible
(`infra/n8n-heberge/`) : Postgres + `sauvegarder.sh` / `restaurer.sh`,
répétition complète le 10 sept. La conservation des exécutions se règle par
`EXECUTIONS_DATA_PRUNE` / `EXECUTIONS_DATA_MAX_AGE`
(https://docs.n8n.io/hosting/scaling/execution-data/) — **aucune purge n'a
été faite sur la base vive**. Le sondage à 2 minutes produit ≈2 900
exécutions/jour pour le socle ; cela établit un volume, **pas** une cause de
corruption SQLite : aucune affirmation causale n'est retenue.
