# Plan n8n — inventaire vérifié et architecture retenue

14 septembre 2026. Relevé sur **l'instance vive** (conteneur `nexora-n8n`,
`n8n export:workflow --all`, lecture seule, 20 définitions) et sur les exports
versionnés (`n8n/socle-envois/*.json`, `n8n/assistant-garage/*`). Rien n'a été
importé, activé ni modifié dans l'instance vive.

## 1. Ce qui tourne réellement (instance vive, 14 sept. 2026)

**6 actifs sur 20.** Les autres sont archivés (n8n `isArchived`, non
réactivables sans désarchivage) ou inactifs.

| Chemin | Workflow vif | Actif | Déclencheur | Source réelle | Résolution du garage | Destinataire | Garde d'autorisation | États | Service externe | Export versionné | Preuve | Coût | Dépendance restante |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| Nouveau devis | `Nouveau devis (socle)` `X39OgaEUulqhv1hO` | **oui** | cron `*/2` (≈720 exécutions/jour) | `notifications_devis` via `reserver_notifications('devis')` | par le document (`devis.garage_id`), tous garages | `destinataire_valide` figé à l'autorisation | `autoriser_envoi_devis` (dirigeant/accueil) ; depuis `20260919000100` refuse le chiffrage incomplet ; `empreinte_devis` intègre lignes + état | sans_lien → en_attente → envoi_en_cours → envoye / bloque / a_reprendre | SMTP Brevo (30/h) | `n8n/socle-envois/nouveau-devis.json` | envoi réel prouvé le 8-9 sept. (mémo) | Brevo gratuit ; Mac hôte | hébergement permanent (Mac endormi = rien ne part) |
| Facture | `Facture (socle)` `9IG1g2ZmHQzhnsMS` | **oui** | cron `*/2` | `notifications_factures` | idem | idem | `autoriser_envoi_facture` | idem | SMTP Brevo | `facture.json` | preuve d'envoi Brevo **à faire** (mémo 12 sept.) | idem | idem |
| Proposition de RDV | `Proposition RDV (socle)` `HdO63GrT2WfopDQP` | **oui** | cron `*/2` | `notifications_proposition` | idem | e-mail client | autorisation à vérifier dans le code du socle (hors périmètre de cette nuit) | idem | SMTP Brevo | `proposition-rdv.json` | mémo 8 sept. : marquait « envoyé » sans e-mail — corrigé dans le socle du 9 | idem | idem |
| Véhicule prêt | `Véhicule prêt (socle)` `jXsssqkdKFR3Hnf9` | **oui** | cron `*/2` | `notifications_atelier` | idem | `destinataire_valide` | `autoriser_envoi_atelier` (depuis 20260918000300, naît `sans_lien`) | idem | SMTP Brevo | `vehicule-pret.json` | envoi réel le 11 sept. (mémo) | idem | **deux composeurs** (voir §3) |
| Détection no-show | `3 - Détection no-show` `jnDj8Aj9DmuLV5Di` | oui | cron horaire | `rendez_vous` | ? (4 nœuds, pas d'envoi) | aucun envoi | — | — | — | non versionné | aucune | — | à documenter ou archiver |
| Journal d'erreurs | `3 - Journalisation erreurs` | oui | `errorTrigger` | erreurs n8n | — | aucun envoi | — | — | — | non versionné | — | — | — |
| Confirmation / modification / annulation RDV | — | **non** (archivé dans `Assistant Garage`) | — | `confirmations_rappels_file`, `confirmations_jetons` existent en base | — | — | — | — | — | `n8n/assistant-garage/*` | aucune | — | activation = tout l'Assistant |
| Acceptation / refus du devis | — (branches `webhook /devis-accepte`, archivées) | **non** | — | depuis 20260917000100 la réponse naît `sans_lien` dans `notifications_devis` (type accepte/refuse) et passe par le socle « Nouveau devis » **si autorisée** | par document | idem | `autoriser_envoi_devis` | idem | — | — | — | — | — |
| Relances de travaux différés | — | **non** (non importé) | cron `*/15` (relève bornée) | `relances_travaux` (20260919000400, 000600, 001000) | par `travaux_differes.garage_id` ; Production : tous garages (`p_garages: null`) | `destinataire_valide` figé à l'autorisation, relu à la réservation | `autoriser_envoi_relance_travail` (dirigeant/accueil, message par message) ; refus si client opposé (journal `revenue_recovery_permissions`) | a_relire → en_attente → envoi_en_cours → envoye / bloque / a_reprendre ; obsolete / annulee ; incertain = reste envoi_en_cours | SMTP Brevo (Prod) / simulé (Test) / **SMTP contrôlé (recette)** | `n8n/relances-travaux/{test,production,recette-smtp}.json` | 34 + 19 contrôles serveur ; variante simulée exécutée (14 sept.) ; **vraie variante exécutée contre un SMTP contrôlé** (15 sept.) ; réception réelle non testée | 0 | **activation bloquée** : saisie de l'opposition, moyen de s'opposer dans le message, base légale, garage volontaire, hébergement (§6) |
| Demande d'avis Google | — (branche `scheduleTrigger 0 9` de l'Assistant, archivée) | **non** | — | `rendez_vous.avis_demande` | — | — | aucune (échouait chaque jour faute de destinataire, mémo) | — | — | — | — | — | lien d'avis par garage + retrait persistant + règles officielles : **non commencé**, branche indisponible |
| Demandes entrantes IMAP / formulaire / WhatsApp | — (`emailReadImap`, `webhook /demande-site`, `/demande-whatsapp` de l'Assistant, archivées ; `RECETTE — Assistant Garage` ×2 inactifs) | **non** | — | boîte Gmail `lkckirua@…` (codée en dur) | **par l'expéditeur du message** — inacceptable (voir §4) | alerte interne vers `lkckirua@…` | aucune | — | Gmail/IMAP, Twilio non configuré | `n8n/assistant-garage/*` | preuves du 10 sept. (mémo) | Gmail | connecteur authentifié **par garage** ; entrées = données non fiables |
| Alerte interne | — | **non** | — | — | — | `lkckirua@…` en dur | — | — | — | — | — | — | — |

Ce que l'inventaire corrige par rapport aux anciens rapports : le nombre de
workflows (20, dont 6 actifs, dont 2 sans envoi) ; « Véhicule prêt » **est**
actif ; les branches confirmation/annulation/avis/IMAP ne tournent **pas**.

## 2. Architecture retenue, appliquée à UN chemin

La base garde documents, autorisations, files et états. Le dashboard prépare,
fait relire, autorise, affiche. n8n **réserve** ce qui est autorisé et
**transporte** ; il ne recalcule aucune règle, ne résout aucun garage, ne
compose aucun message.

Le chemin représentatif : **la relance d'un travail différé**
(`20260919000400_relances_travaux_differes.sql`).

```
candidat        travaux_differes échu (statut planifie / a_relancer, date_relance ≤ aujourd'hui)
brouillon       preparer_relances_travaux()      → relances_travaux.a_relire   [service_role]
autorisation    autoriser_envoi_relance_travail() → en_attente                 [dirigeant / accueil]
réservation     reserver_relances_travaux()      → envoi_en_cours (SKIP LOCKED) [service_role]
tentative       n8n : SMTP (prod) / Code simulé (test)
résultat        terminer_relance_travail()       → envoye | bloque | a_reprendre (→ en_attente, tentatives < 3)
```

Contrat d'une ligne : `garage_id` (identité du garage), `travail_differe_id`
+ `echeance` (identité d'idempotence — une relance par travail et par
échéance ; un report crée une autre échéance, donc une autre relance
possible, jamais un doublon accidentel), `statut`, `motif` /
`derniere_erreur`, `destinataire_valide`, `empreinte_document`,
`autorise_par`, `tentatives`.

Conditions **relues à la réservation** : travail encore ouvert, échéance
inchangée, destinataire inchangé, message inchangé. Ce qui a bougé est mis de
côté (`bloque`) avec le motif, et **revalidé à la main**.

`envoi_en_cours` n'est jamais recyclé par un délai. Un timeout après une
acceptation possible du fournisseur reste incertain ; l'écran dit « Envoi à
vérifier ». Pas de promesse « exactement une fois » avec SMTP.

**Une seule composition** : `composer_relance_travail()` en base, au moment de
la préparation. L'aperçu de l'écran et l'e-mail sont le même texte. C'est la
réponse au constat des « deux composeurs » de « véhicule prêt ».

### Réveil événementiel : non testé, et pourquoi

Les webhooks de base Supabase peuvent réveiller un consommateur, mais ils ne
font pas une file durable, et le Mac hôte endormi ne reçoit rien. Le workflow
livré est une **relève périodique bornée** (`*/15`), qui retrouve ce qui a été
manqué. Aucun tunnel public n'a été ouvert. Le réveil événementiel est
documenté comme **non testé**.

### Mesure

Sur Test, le chemin a été joué par script (`relances-travaux-serveur.mjs`),
pas par n8n : 32 contrôles, dont la concurrence de réservation et le blocage
après modification. **Nombre d'exécutions n8n utiles/vides : non mesuré**, le
workflow n'ayant pas tourné. À `*/15`, une tournée fait 96 exécutions/jour par
workflow, la plupart vides ; c'est le coût à mesurer sur une fenêtre connue
avant toute extrapolation.

## 3. Les deux composeurs de « véhicule prêt » — revérifié

Toujours vrai au 14 septembre : le nœud Code du workflow vif
(`jXsssqkdKFR3Hnf9`) et `apercu_message_atelier` (SQL) composent chacun le
message. `scripts/recette/compare-apercu-atelier.mjs` les compare (0 écart le
13 sept.). Ce lot **n'a pas** modifié ce chemin : correction à faire en
faisant lire au nœud n8n le texte rendu par la base (comme pour les relances),
avec validation fraîche de la version et du destinataire.

## 4. IMAP, avis Google : conditions, pas d'interrupteur

- **IMAP** : pas de garage de secours, pas de routage sur l'expéditeur libre.
  L'identité de la boîte/connecteur autorisé doit déterminer le garage ; un
  transfert partagé ne prouve rien. Messages et pièces jointes = entrées non
  fiables, jamais des instructions. Activation après connecteur authentifié
  **par garage** et recette de livraison réelle. **Rien de livré cette nuit.**
- **Avis Google** : lien d'avis propre au garage (`garages.lien_avis_google`
  existe), retrait persistant (n'existe pas), règle d'éligibilité, aucune
  sélection des seuls satisfaits, aucune récompense. **Non commencé** ; la
  branche reste indisponible (`CAPACITES.demandeAvis.disponible = false`).

## 5. Exports livrés

- `n8n/relances-travaux/test.json` — Supabase **Test**, identifiant
  `RPC Supabase RECETTE (Test)` (`BmVzKJSWPhAJ5V3Z`), **borné au garage de
  recette** (`p_garages`), transport **simulé** (nœud Code : refuse tout
  destinataire hors `.invalid`, n'envoie rien, clôt `envoye` avec le motif
  « transport simulé »). Inactif à l'import (n8n importe tout inactif).
- `n8n/relances-travaux/production.json` — Supabase Production,
  `RPC Supabase Production` + `SMTP Brevo — envois métier`, **inactif**,
  tous garages (`p_garages: null`), `onError: continueErrorOutput` sur l'envoi
  → `a_reprendre` borné.
- Générés par `n8n/relances-travaux/construire.mjs` : un seul source, deux
  variantes. Identifiants de workflow **que rien ne porte** dans l'instance
  vive (`relancestravaux{test,prod}0000001`), aucune clé dans les fichiers.

Vérifié dans les fichiers : URL (Test/Prod), identifiants distincts,
déclencheur `*/15`, garde `p_garages` sur Test, absence de collision d'id.
**Exécuté le 14 sept. (soir)** : variante Test importée seule dans une
instance n8n **isolée** (`nexora-n8n-recette`, sans lien avec l'instance
vive), trois passages : préparation, autorisation depuis le dashboard,
réservation, résultat simulé, état visible. Échec certain repris, issue
incertaine jamais close ni reprise, report → `obsolete`, annulation non
réservée. Détail : `docs/recette/n8n-relances-execution-2026-09-14.md`.
**Workflow exécuté avec transport simulé ; réception réelle non testée** ; le
classement SMTP de la variante Production n'a pas été exécuté.

### Vraie variante contre un SMTP contrôlé (15 sept. 2026)

`recette-smtp.json` = les nœuds de `production.json` (vérifié : mêmes nœuds,
plus le déclencheur manuel), sur Test, borné à un garage neuf, identifiant SMTP
visant `scripts/recette/smtp-controle.mjs` (127.0.0.1, n'accepte que
`@nexora-recette.invalid`, ne relaie rien). Deux passages :

| Cas | Passage 1 | Passage 2 |
|---|---|---|
| accepté (250 après le message) | `envoye` | non repris, rien renvoyé |
| 451 au RCPT TO | `en_attente`, tentatives 1 | repris, tentatives 2 |
| 550 au RCPT TO | `bloque` | non repris |
| message reçu puis connexion coupée | `envoi_en_cours` (incertain, rien clos) | **non repris** |

n8n ne transmet au nœud d'échec que le **texte** de l'erreur
(`{ error: "Connection closed unexpectedly" }`, sans code SMTP structuré) :
`classerEchec.js` ne lit donc que les formulations de nodemailer **ancrées en
début de message** (« all recipients were rejected: 451 », « Message failed:
554 »…), jamais un nombre trouvé ailleurs ; tout le reste est incertain
(8 tests unitaires).

## 6. Fiche de chaque automatisation concernée

**Relance de travail différé** (implémentée et testée, non publiée, activation bloquée)
- *Déclencheur* : relève `*/15` ; aucun envoi à l'échéance seule.
- *Autorisation* : le garage relit et autorise chaque message
  (`autoriser_envoi_relance_travail`, dirigeant ou accueil) ; refusée si le
  client est opposé ; relue à la réservation (travail ouvert, échéance,
  destinataire, texte, opposition).
- *Destinataire* : l'e-mail du client figé à l'autorisation ; changé depuis →
  mis de côté.
- *Expéditeur* : `"<nom du garage>" <nexorasolutions.france@gmail.com>` (identité
  Brevo vérifiée, même que le socle), Reply-To = e-mail du garage s'il est valide.
- *Doublons* : une relance par (travail, échéance) ; réservation atomique
  `SKIP LOCKED` ; `envoi_en_cours` jamais repris.
- *Erreurs* : refus certain avant ou après le message → `a_reprendre` (4xx,
  3 tentatives) ou `bloque` (5xx) ; coupure, délai, inconnu → incertain, reste
  `envoi_en_cours`, écran « envoi à vérifier ».
- *Manques qui bloquent l'activation* : aucun écran pour enregistrer une
  opposition (la fonction `revenue_recovery_enregistrer_permission` existe,
  réservée au propriétaire) ; aucun moyen de s'opposer dans le message ; base
  légale à confirmer ; pas d'activation par garage (Production = tous les
  garages) ; réception réelle via Brevo non prouvée.

**Nouveau devis (socle)** (publié, actif)
- *Déclencheur* : cron `*/2`. *Autorisation* : `autoriser_envoi_devis`, refusée
  si chiffrage incomplet ; **l'empreinte signe désormais le contenu lu par le
  client** (001000) : libellé, constat ou photo modifiés → mis de côté,
  revalidation. *Destinataire* : figé. *Expéditeur* : même identité.
  *Doublons* : réservation atomique. *Erreurs* : selon le workflow publié
  (non modifié par ce lot).

**Dépendance au Mac** : l'instance n8n vive (`nexora-n8n`) tourne dans Docker
sur le Mac de Baptiste. Mac éteint ou endormi : aucun envoi du socle, aucune
relance ; au réveil, la relève reprend ce qui est autorisé (rien ne part deux
fois, des messages peuvent partir en retard). La pile hébergée
(`infra/n8n-heberge/`) est préparée, **non migrée** ; aucun changement cette nuit.

## 7. Conditions de passage en Production (pour le plan de publication)

1. Migrations `20260919000100` → `001000` (voir la proposition de publication).
2. **Ne pas importer** `production.json` tant que les manques du §6 ne sont pas
   levés.
3. Le jour venu : import dans un workflow **neuf** (« Import from File »),
   identifiants vérifiés, **inactif** ; `p_garages` restreint au garage
   volontaire ; `preparer_relances_travaux()` une fois à la main ; relecture et
   autorisation par le garage ; activation et observation d'une fenêtre connue.
4. Arrêt sûr : désactiver le workflow ; les lignes `en_attente` restent en
   attente, rien ne part. **Pas de suppression de table ni de fonction** : les
   relances et leur historique restent (voir la proposition, §6).

## 8. Sauvegarde, restauration, exécutions

Instance vive : SQLite dans le conteneur, sauvegarde par `n8n export:workflow
--all --backup` (dernier export complet : `Desktop/Nexora - Automatisation/
Workflows n8n/export-complet-2026-09-11/`) et copie du volume. Pile cible
(`infra/n8n-heberge/`) : Postgres + `sauvegarder.sh` / `restaurer.sh`,
répétition complète le 10 sept. La conservation des exécutions se règle par
`EXECUTIONS_DATA_PRUNE` / `EXECUTIONS_DATA_MAX_AGE`
(https://docs.n8n.io/hosting/scaling/execution-data/) — **aucune purge n'a
été faite sur la base vive**. Le sondage à 2 minutes produit ≈2 900
exécutions/jour pour le socle ; cela établit un volume, **pas** une cause de
corruption SQLite : aucune affirmation causale n'est retenue.

## 9. Fiabilisation du socle — 15 septembre 2026 (soir)

**État : préparé et testé en recette isolée ; rien n'est actif.** Instance
vive et Production non modifiées (lecture seule). Bascule :
`n8n/socle-envois/BASCULE-A-AUTORISER.md`. Preuves :
`docs/recette/n8n-fiabilisation-2026-09-15.md`.

### 9.1 Ce qui a été relevé (lecture seule, 15 sept. ~18 h 30)

- 6 actifs, versions publiées : voir la procédure §0. `errorWorkflow` absent
  des 6 : le journaliseur n'a jamais tourné.
- **Historique n8n = 4 jours, pas 7** : aucun réglage `EXECUTIONS_*`, donc
  défauts de n8n 2.37.7 (conservation 336 h **et 10 000 exécutions au plus**).
  10 077 exécutions depuis le 11 sept. 19 h 28. Le « ≈ 18 000 en 7 jours » de
  l'inventaire était une extrapolation ; la mesure est ≈ 2 600/jour pour le
  socle, Mac éveillé. SQLite 144 Mo (+ ≈ 230 Mo de copies anciennes, non
  purgées).
- 16 exécutions en erreur dans la fenêtre, **toutes au nœud « Réserver la
  file »**, les 4 workflows à la même minute : 12 « connexion impossible »,
  3 « connexion fermée », 1 Gateway Timeout. Production : 0 ligne
  `en_attente` ou `envoi_en_cours` — rien n'est resté bloqué.
- Veille du Mac visible : trous de 10 à 69 min (13-14 et 15 sept.) ; la
  détection no-show horaire saute les heures manquées.
- Production `erreurs_automatisation` : 324 lignes, toutes de l'Assistant
  archivé (nœud « Inconnu »), dernière le 9 sept.

### 9.2 Causes établies

1. Le nœud d'envoi n'avait **aucune sortie d'erreur** : un refus 451/550 ou une
   coupure arrêtait l'exécution et laissait la ligne **et jusqu'à 9 lignes
   réservées jamais tentées** en `envoi_en_cours`. « Clore bloqué » et « Clore
   à reprendre » n'étaient reliés à rien.
2. Lecture ou composition en échec : même arrêt, ligne incertaine alors que
   rien n'était parti.
3. `terminer_notification('a_reprendre')` remet en attente **sans plafond**.
4. Journaliseur : garage codé en dur, coquille `return out;_`, message brut
   (adresses, liens), aucun regroupement, jamais rattaché.
5. Réservation sans délai d'attente ; une réponse perdue après validation laisse
   des lignes réservées sans envoi.

### 9.3 Correctifs (source unique `n8n/socle-envois/construire.mjs`)

Mêmes ids, même composition des messages, mêmes lectures. Une ligne réservée
à la fois, 3 au plus par passage, arrêt après un refus temporaire ; délai de
20 s sur la réservation, sans reprise ; échec avant envoi = « rien n'est
parti » ; classement SMTP partagé avec les relances (`classerEchec.js`) ;
plafond de 3 tentatives ; une clôture rejouée 3 fois (idempotente) ; toute
issue autre qu'« envoyé » journalisée après la clôture, sans bloquer la file ;
workflow d'erreur rattaché. Journal : migration additive
`20260921000100_journal_incidents_n8n.sql` (colonnes + `journaliser_incident`,
regroupement par signature sur 24 h), journaliseur corrigé en place.

| Situation | Issue en file | Journal |
|---|---|---|
| file vide | — | — |
| réseau coupé avant réservation | rien réservé | `reseau_avant_reservation` (regroupé) |
| réservation sans réponse | ≤ 1 ligne `envoi_en_cours` | `reservation_sans_reponse`, intervention |
| lecture impossible | `en_attente` (reprise) | `echec_avant_envoi` |
| document ou adresse manquants | `bloque` | `donnees_invalides`, intervention |
| 4xx | `en_attente`, puis `bloque` à 3 tentatives | `refus_temporaire`, puis `…_repete` |
| 5xx | `bloque` | `refus_definitif`, intervention |
| coupure, délai, inconnu après envoi possible | `envoi_en_cours`, jamais repris | `envoi_incertain`, intervention |
| accepté, clôture impossible | `envoi_en_cours`, jamais repris | `cloture_echouee`, intervention |

### 9.4 Fréquences

| Workflow | Actuel | Proposé | Délai client max, Mac éveillé | Passages/jour |
|---|---|---|---|---|
| Nouveau devis | `*/2` | **`*/2` gardé** | 2 min : l'envoi est souvent annoncé au client au comptoir ou au téléphone | 720 |
| Facture | `*/2` | `*/5` | 5 min : la facture est remise au comptoir, le courriel est une copie | 720 → 288 |
| Proposition RDV | `*/2` | `*/5` | 5 min : le client n'attend pas en ligne | 720 → 288 |
| Véhicule prêt | `*/2` | `*/5` | 5 min, sur un délai d'atelier de plusieurs heures | 720 → 288 |
| Détection no-show, journaliseur | horaire / sur erreur | inchangés | — | — |

Économie à périmètre constant : 2 880 → 1 584 passages/jour (−45 %). Passage
mesuré : 0,5 s en moyenne, 60 s au pire en erreur réseau → aucun
chevauchement ; deux passages simultanés sont de toute façon séparés par
`SKIP LOCKED` (recette). File accumulée : 3 lignes par passage, soit 36/h à
`*/5` et 90/h à `*/2` par file ; Brevo plafonne à 30/h (au-delà, 4xx →
reprise, puis blocage à la 3e tentative : à surveiller si le volume monte).
**Un intervalle plus long ne résout pas la veille du Mac.**

### 9.5 Limites restantes

- Mac endormi ou éteint : rien ne part ; au réveil, rattrapage 3 lignes par passage.
- Le journal dépend de Supabase comme la réservation ; recours
  `scripts/n8n/incidents-locaux.sh` (lecture de l'historique n8n, 10 000
  exécutions au plus).
- Une erreur de workflow après la réservation (défaut de nœud) immobilise une
  ligne par passage en `envoi_en_cours` : visible et regroupée, jamais renvoyée,
  mais à vérifier à la main.
- Alertes : requête prête, **aucun destinataire choisi, rien n'est envoyé**.
- Conservation de l'historique n8n inchangée (décision séparée : régler
  `EXECUTIONS_DATA_PRUNE_MAX_COUNT` demande de recréer le conteneur vif).
